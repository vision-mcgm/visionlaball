function [  ] = grabAndExec( fHandle )
%Runs on a cluster node. Takes a function handle and an operation-group of
%parameters. Runs the function once for each of the operations
%Assumes args is a struct COLVEC with OPS rows
%control is a struct created by SliceAndSend with

for i=1:n
    

end

